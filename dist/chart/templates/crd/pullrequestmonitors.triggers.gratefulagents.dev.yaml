{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: pullrequestmonitors.triggers.gratefulagents.dev
spec:
  group: triggers.gratefulagents.dev
  names:
    kind: PullRequestMonitor
    listKind: PullRequestMonitorList
    plural: pullrequestmonitors
    singular: pullrequestmonitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .spec.number
      name: PR
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.lastPollTime
      name: LastPoll
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PullRequestMonitor is restart-safe polling state for an AgentRun-owned
          pull request.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PullRequestMonitorSpec defines the immutable identity of
              a pull request monitor.
            properties:
              discoveredAt:
                format: date-time
                type: string
              githubRepositoryRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              implementerRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              number:
                format: int32
                minimum: 1
                type: integer
              repository:
                pattern: ^[^/[:space:]]+/[^/[:space:]]+$
                type: string
              url:
                pattern: ^https://github\.com/[^/]+/[^/]+/pull/[1-9][0-9]*$
                type: string
            required:
            - discoveredAt
            - implementerRef
            - number
            - repository
            - url
            type: object
            x-kubernetes-validations:
            - message: implementerRef.name is required
              rule: size(self.implementerRef.name) > 0
          status:
            description: PullRequestMonitorStatus defines the observed state of a
              pull request monitor.
            properties:
              authorLogin:
                type: string
              baseRef:
                type: string
              checks:
                description: PullRequestMonitorHeadRollup is a check or commit-status
                  result bound to one head SHA.
                properties:
                  count:
                    format: int32
                    type: integer
                  error:
                    type: string
                  headSHA:
                    type: string
                  observedAt:
                    format: date-time
                    type: string
                  state:
                    type: string
                type: object
              commentsError:
                type: string
              commentsObservedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveErrors:
                format: int32
                type: integer
              etags:
                description: |-
                  PullRequestMonitorETags holds conditional-request validators that are safe to
                  reuse without interfering with paginated event processing.
                properties:
                  pull:
                    type: string
                type: object
              headRef:
                type: string
              headSHA:
                type: string
              lastError:
                type: string
              lastIssueCommentCursor:
                description: GitHubObjectCursor identifies the latest processed GitHub
                  object.
                properties:
                  id:
                    format: int64
                    type: integer
                  timestamp:
                    format: date-time
                    type: string
                required:
                - id
                - timestamp
                type: object
              lastPollTime:
                format: date-time
                type: string
              lastReviewCursor:
                description: GitHubObjectCursor identifies the latest processed GitHub
                  object.
                properties:
                  id:
                    format: int64
                    type: integer
                  timestamp:
                    format: date-time
                    type: string
                required:
                - id
                - timestamp
                type: object
              lifecycle:
                description: PullRequestLifecycle is the GitHub lifecycle independently
                  observed for a pull request.
                enum:
                - open
                - draft
                - merged
                - closed
                type: string
              mergeability:
                description: PullRequestMergeability is GitHub's current mergeability
                  result.
                enum:
                - unknown
                - mergeable
                - conflicting
                type: string
              mergedAt:
                format: date-time
                type: string
              openedDispatched:
                type: boolean
              pullError:
                type: string
              pullObservedAt:
                format: date-time
                type: string
              rateLimitRemaining:
                format: int32
                type: integer
              rateLimitReset:
                format: date-time
                type: string
              retryAfter:
                format: date-time
                type: string
              reviewDecision:
                description: PullRequestReviewDecision is the aggregate latest review
                  result.
                enum:
                - unknown
                - approved
                - changes_requested
                - review_required
                type: string
              reviewsError:
                type: string
              reviewsObservedAt:
                format: date-time
                type: string
              state:
                description: PullRequestMonitorState is the review-loop state associated
                  with a monitored pull request.
                enum:
                - pending
                - open
                - resolving
                - approved
                - blocked
                - merged
                - closed
                - cancelled
                - inactive
                type: string
              statuses:
                description: PullRequestMonitorHeadRollup is a check or commit-status
                  result bound to one head SHA.
                properties:
                  count:
                    format: int32
                    type: integer
                  error:
                    type: string
                  headSHA:
                    type: string
                  observedAt:
                    format: date-time
                    type: string
                  state:
                    type: string
                type: object
              title:
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: spec is immutable
          rule: self.spec == oldSelf.spec
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
