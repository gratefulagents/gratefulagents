---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: runtimeprofiles.platform.gratefulagents.dev
spec:
  group: platform.gratefulagents.dev
  names:
    kind: RuntimeProfile
    listKind: RuntimeProfileList
    plural: runtimeprofiles
    singular: runtimeprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.security.permissionMode
      name: Permission
      type: string
    - jsonPath: .spec.security.egressMode
      name: Egress
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuntimeProfile is the Schema for the runtimeprofiles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuntimeProfileSpec defines the desired state of RuntimeProfile.
            properties:
              admission:
                description: RuntimeProfileAdmission holds lightweight concurrency
                  defaults for v1.
                properties:
                  maxConcurrentRuns:
                    format: int32
                    type: integer
                  perNamespaceMaxConcurrentRuns:
                    format: int32
                    type: integer
                  staleRunTimeout:
                    type: string
                type: object
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              sandbox:
                description: RuntimeProfileSandbox holds sandbox provisioning defaults.
                properties:
                  commandSandbox:
                    description: |-
                      CommandSandbox configures the same-pod subprocess sandbox used by model-controlled commands.
                      Toolchains are mounted read-only; the workspace remains the writable project boundary.
                    properties:
                      env:
                        additionalProperties:
                          type: string
                        description: |-
                          Env adds non-secret environment variables to sandboxed subprocesses.
                          Values can reference other safe sandbox variables such as $PATH; secret-like keys are ignored by the controller.
                        type: object
                      extraReadOnlyPaths:
                        description: |-
                          ExtraReadOnlyPaths mounts additional host paths read-only inside the subprocess sandbox.
                          Paths inside /workspace, /tmp, /proc, /dev, /run, /var/run, /var/lib, /home, /root, and / are ignored by the subprocess runtime.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      extraWritablePaths:
                        description: |-
                          ExtraWritablePaths grants absolute admin-owned scratch or cache paths writable inside the subprocess sandbox.
                          Workspace paths are not allowed, and sensitive roots are ignored by the subprocess runtime.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      path:
                        description: |-
                          Path replaces the default subprocess PATH. Use absolute paths only; writable and sensitive paths are ignored.
                          When set, PathPrepend and PathAppend are ignored by the subprocess runtime.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      pathAppend:
                        description: |-
                          PathAppend adds absolute paths after the default subprocess PATH.
                          Sensitive paths are ignored; project-local node_modules/.bin and the dedicated scratch Go bin path are allowed here so project tools resolve after system tools.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      pathPrepend:
                        description: PathPrepend adds absolute paths before the default
                          subprocess PATH. Writable and sensitive paths are ignored.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  enablePrivateProcfs:
                    description: |-
                      EnablePrivateProcfs runs the worker pod in a Kubernetes user namespace and
                      unmasks the worker container's procfs so bubblewrap can mount a fresh,
                      PID-namespaced /proc for model-controlled commands. This is required by
                      tools such as Chromium and Go that depend on /proc/self or /proc/cpuinfo.
                      It requires node, container-runtime, and volume support for Kubernetes pod
                      user namespaces and a Pod Security policy that permits Unmasked procMount.
                      The worker's existing procfs is never exposed inside the command sandbox.
                    type: boolean
                  persistWorkspace:
                    description: |-
                      PersistWorkspace enables a PersistentVolumeClaim for /workspace so that
                      files survive pod restarts (e.g. pause/resume). Default: false (EmptyDir).
                    type: boolean
                  runtimeClassName:
                    type: string
                  sandboxTemplateRef:
                    description: NamedRef references another namespaced resource by
                      name.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  warmPoolRef:
                    description: NamedRef references another namespaced resource by
                      name.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  workspaceSize:
                    description: |-
                      WorkspaceSize sets the PVC capacity when PersistWorkspace is true.
                      Default: "10Gi".
                    pattern: ^[0-9]+(\.[0-9]+)?(Ki|Mi|Gi|Ti|Pi|Ei|m|k|M|G|T|P|E)?$
                    type: string
                type: object
              security:
                description: RuntimeProfileSecurity holds executor governance defaults.
                properties:
                  defaultTimeout:
                    type: string
                  egressMode:
                    description: EgressMode defines outbound network posture.
                    enum:
                    - unrestricted
                    - restricted
                    - disabled
                    type: string
                  gitRemoteWrites:
                    description: |-
                      GitRemoteWrites controls whether the agent may push to Git remotes,
                      including through built-in tools, compound PR tools, and shell commands.
                      Defaults to enabled for backward compatibility.
                    enum:
                    - enabled
                    - disabled
                    type: string
                  permissionMode:
                    description: PermissionMode defines the runtime mutability boundary.
                    enum:
                    - read-only
                    - workspace-write
                    - danger-full-access
                    type: string
                type: object
            type: object
          status:
            description: RuntimeProfileStatus defines the observed state of RuntimeProfile.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                type: string
              resolvedDefaultsHash:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
