---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: maintainerworkitems.triggers.gratefulagents.dev
spec:
  group: triggers.gratefulagents.dev
  names:
    kind: MaintainerWorkItem
    listKind: MaintainerWorkItemList
    plural: maintainerworkitems
    singular: maintainerworkitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repositoryRef.name
      name: Repository
      type: string
    - jsonPath: .spec.issueNumber
      name: Issue
      type: integer
    - jsonPath: .spec.disposition
      name: Disposition
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MaintainerWorkItem is durable triage state for a repository issue.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaintainerWorkItemSpec defines the stable issue identity
              and accepted triage intent.
            properties:
              acceptedScope:
                description: MaintainerAcceptedScope records the scope accepted by
                  triage.
                properties:
                  acceptanceCriteria:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  statement:
                    type: string
                type: object
              children:
                items:
                  description: MaintainerWorkItemReference identifies a work item
                    in the same namespace.
                  properties:
                    name:
                      minLength: 1
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              closeReason:
                description: MaintainerWorkItemCloseReason is the GitHub close reason
                  selected by triage.
                enum:
                - not_planned
                - completed
                type: string
              dependencies:
                items:
                  description: MaintainerWorkItemReference identifies a work item
                    in the same namespace.
                  properties:
                    name:
                      minLength: 1
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              disposition:
                description: MaintainerWorkItemDisposition is the accepted triage
                  disposition for an issue.
                enum:
                - NotActionable
                - Bounded
                - Decomposable
                - Discovery
                - Escalated
                type: string
              evidenceSummary:
                minLength: 1
                type: string
              issueNumber:
                format: int32
                minimum: 1
                type: integer
              repositoryRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              requiredPullRequests:
                items:
                  description: MaintainerRequiredPullRequestIntent identifies a pull
                    request required for delivery.
                  properties:
                    name:
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              triagedByCommand:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - issueNumber
            - repositoryRef
            type: object
            x-kubernetes-validations:
            - message: repositoryRef and issueNumber are immutable
              rule: self.repositoryRef == oldSelf.repositoryRef && self.issueNumber
                == oldSelf.issueNumber
            - message: closeReason is required when disposition is NotActionable
              rule: '!has(self.disposition) || self.disposition != ''NotActionable''
                || has(self.closeReason)'
            - message: closeReason is only valid when disposition is NotActionable
              rule: '!has(self.closeReason) || (has(self.disposition) && self.disposition
                == ''NotActionable'')'
          status:
            description: MaintainerWorkItemStatus defines the observed issue state
              and execution progress.
            properties:
              agentRuns:
                items:
                  description: MaintainerWorkItemAgentRunProjection summarizes an
                    AgentRun associated through work-item identity labels.
                  properties:
                    name:
                      minLength: 1
                      type: string
                    observedAt:
                      format: date-time
                      type: string
                    phase:
                      type: string
                    prLoopRound:
                      format: int32
                      type: integer
                    prLoopState:
                      type: string
                    role:
                      description: MaintainerWorkItemAgentRunRole identifies the role
                        of a projected AgentRun.
                      enum:
                      - Implementer
                      - Reviewer
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  - role
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              children:
                items:
                  description: MaintainerWorkItemChildProjection summarizes a child
                    work item.
                  properties:
                    delivered:
                      type: boolean
                    name:
                      minLength: 1
                      type: string
                    observedAt:
                      format: date-time
                      type: string
                    phase:
                      description: MaintainerWorkItemPhase is the lifecycle phase
                        of a work item.
                      enum:
                      - PendingTriage
                      - Triaged
                      - AwaitingDecision
                      - ReadyToDispatch
                      - Dispatched
                      - Implementing
                      - ReadyToMerge
                      - Delivered
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dependencies:
                items:
                  description: MaintainerWorkItemDependencyProjection summarizes a
                    dependency work item.
                  properties:
                    delivered:
                      type: boolean
                    name:
                      minLength: 1
                      type: string
                    observedAt:
                      format: date-time
                      type: string
                    phase:
                      description: MaintainerWorkItemPhase is the lifecycle phase
                        of a work item.
                      enum:
                      - PendingTriage
                      - Triaged
                      - AwaitingDecision
                      - ReadyToDispatch
                      - Dispatched
                      - Implementing
                      - ReadyToMerge
                      - Delivered
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              dispatchReservation:
                description: MaintainerDispatchReservation records capacity reserved
                  before dispatch side effects.
                properties:
                  agentRunRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  commandRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  expiresAt:
                    format: date-time
                    type: string
                  id:
                    minLength: 1
                    type: string
                  reservedAt:
                    format: date-time
                    type: string
                required:
                - commandRef
                - id
                - reservedAt
                type: object
              issueObservation:
                description: MaintainerIssueObservation is a durable observation of
                  a GitHub issue.
                properties:
                  authorLogin:
                    type: string
                  bodyHash:
                    type: string
                  githubUpdatedAt:
                    format: date-time
                    type: string
                  labels:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  number:
                    format: int32
                    type: integer
                  observedAt:
                    format: date-time
                    type: string
                  state:
                    description: MaintainerIssueState is the observed GitHub issue
                      state.
                    enum:
                    - open
                    - closed
                    type: string
                  title:
                    type: string
                  url:
                    type: string
                required:
                - authorLogin
                - bodyHash
                - githubUpdatedAt
                - labels
                - number
                - observedAt
                - state
                - title
                - url
                type: object
              pendingDecision:
                description: MaintainerPendingDecision records an unresolved authorized
                  decision request.
                properties:
                  id:
                    minLength: 1
                    type: string
                  options:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  question:
                    minLength: 1
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  requestedByCommand:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - id
                - question
                - requestedAt
                type: object
              phase:
                description: MaintainerWorkItemPhase is the lifecycle phase of a work
                  item.
                enum:
                - PendingTriage
                - Triaged
                - AwaitingDecision
                - ReadyToDispatch
                - Dispatched
                - Implementing
                - ReadyToMerge
                - Delivered
                type: string
              projectionSequence:
                format: int64
                type: integer
              pullRequests:
                items:
                  description: MaintainerWorkItemPullRequestProjection summarizes
                    the monitor facts required for readiness.
                  properties:
                    checkState:
                      description: MaintainerWorkItemCheckState summarizes CI and
                        commit-status observations for an exact head.
                      enum:
                      - Unknown
                      - Pending
                      - Passing
                      - Failing
                      type: string
                    checksObservedAt:
                      format: date-time
                      type: string
                    draft:
                      type: boolean
                    fresh:
                      description: Fresh is true only when every authoritative monitor
                        source is complete for HeadSHA.
                      type: boolean
                    headObservedAt:
                      format: date-time
                      type: string
                    headSHA:
                      type: string
                    intentName:
                      description: IntentName is the stable required-PR key (normally
                        the monitor name).
                      minLength: 1
                      type: string
                    mergeable:
                      type: boolean
                    mergedAt:
                      format: date-time
                      type: string
                    monitorRef:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    number:
                      format: int32
                      minimum: 1
                      type: integer
                    observationError:
                      type: string
                    repository:
                      pattern: ^[^/[:space:]]+/[^/[:space:]]+$
                      type: string
                    reviewDecision:
                      type: string
                    reviewObservedAt:
                      format: date-time
                      type: string
                    state:
                      description: MaintainerWorkItemPullRequestState is the observed
                        GitHub lifecycle state of a pull request.
                      enum:
                      - open
                      - closed
                      - merged
                      type: string
                    statusesObservedAt:
                      format: date-time
                      type: string
                    url:
                      type: string
                  required:
                  - intentName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - intentName
                x-kubernetes-list-type: map
              readiness:
                description: MaintainerWorkItemReadiness records controller-computed
                  dispatch and merge readiness.
                properties:
                  observedAt:
                    format: date-time
                    type: string
                  readyToDispatch:
                    type: boolean
                  readyToMerge:
                    type: boolean
                  unmetRequirements:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              resolvedDecision:
                description: MaintainerResolvedDecision audits the authenticated answer
                  that cleared pendingDecision.
                properties:
                  answer:
                    type: string
                  humanSubject:
                    type: string
                  id:
                    type: string
                  resolvedAt:
                    format: date-time
                    type: string
                  resolvedByCommand:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - answer
                - humanSubject
                - id
                - resolvedAt
                - resolvedByCommand
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
