# Windows support

Selected-display desktop control currently requires macOS 15.2 or later.
Other platforms fail closed and expose no capture or input capability. A future
Windows implementation must implement the explicit display/desktop-input consent
contract, not restore the removed window-only or agent-choice modes.
